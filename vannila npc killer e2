@name Vanilla NPC Killer d4

findByClass("npc_*")
foreach(_,NPC:entity = findToArray()){
    local Prop = propSpawn("models/props_phx/ww2bomb.mdl", NPC:pos(), 1)
    Prop:propBreak()
}

interval(1500)
